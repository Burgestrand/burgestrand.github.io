%header
  %h1 {{ page.title }}
  %p
    Publicerad
    %time{:pubdate => 'pubdate', :datetime => "{{ page.date }}"}
      {{ page.date | date: "%d %B %Y" }}
    %a{:rel => 'permalink', :href => "{{ page.url }}"} permalink

{{ content }}